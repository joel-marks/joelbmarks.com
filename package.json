{
  "name": "joelbmarks-site",
  "private": true,
  "type": "module",
  "description": "Tooling for joelbmarks.com — CI paper-PDF generation (Hugo builds the site).",
  "scripts": {
    "generate-pdfs": "node scripts/generate-paper-pdfs.mjs"
  },
  "devDependencies": {
    "playwright": "1.48.0"
  }
}
